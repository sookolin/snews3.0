"""CityNews shared core package.

Contains configuration, infrastructure (database, redis, logging, security),
domain models, schemas, business services and the plugin registry that are
consumed by every service (backend API, telegram bot, workers).
"""

__version__ = "3.0.0"
